dist: trusty
sudo: false
language: cpp

addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        packages:
            - g++-8

script:
    - cd example
    - cmake $(python ./script/cmake_release_flags.py) .
    - cmake --build . -- -j2
